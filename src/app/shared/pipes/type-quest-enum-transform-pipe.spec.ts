import { TypeQuestEnumTransformPipe } from './type-quest-enum-transform-pipe';

describe('TypeQuestEnumTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeQuestEnumTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
